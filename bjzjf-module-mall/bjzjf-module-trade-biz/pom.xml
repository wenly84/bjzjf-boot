<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>site.hansi</groupId>
        <artifactId>bjzjf-module-mall</artifactId>
        <version>${zjf.version}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>bjzjf-module-trade-biz</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        trade 模块，主要实现交易相关功能
        例如：订单、退款、购物车等功能。
    </description>

    <dependencies>
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-module-trade-api</artifactId>
            <version>${zjf.version}</version>
        </dependency>
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-module-product-api</artifactId>
            <version>${zjf.version}</version>
        </dependency>
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-module-pay-api</artifactId>
            <version>${zjf.version}</version>
        </dependency>
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-module-promotion-api</artifactId>
            <version>${zjf.version}</version>
        </dependency>
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-module-member-api</artifactId>
            <version>${zjf.version}</version>
        </dependency>
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-module-system-api</artifactId>
            <version>${zjf.version}</version>
        </dependency>

        <!-- 业务组件 -->
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-biz-tenant</artifactId>
        </dependency>
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-biz-ip</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-redis</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-test</artifactId>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-excel</artifactId>
        </dependency>
    </dependencies>

</project>
