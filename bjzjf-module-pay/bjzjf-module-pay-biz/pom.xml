<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>site.hansi</groupId>
        <artifactId>bjzjf-module-pay</artifactId>
        <version>${zjf.version}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>bjzjf-module-pay-biz</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        pay 模块，我们放支付业务，提供业务的支付能力。
        例如说：商户、应用、支付、退款等等
    </description>

    <dependencies>
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-module-pay-api</artifactId>
            <version>${zjf.version}</version>
        </dependency>

        <!-- 业务组件 -->
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-biz-pay</artifactId>
            <version>${zjf.version}</version>
        </dependency>
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-biz-tenant</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-redis</artifactId>
        </dependency>

        <!-- Job 定时任务相关 -->
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-job</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>site.hansi</groupId>
            <artifactId>bjzjf-spring-boot-starter-excel</artifactId>
        </dependency>
    </dependencies>

</project>
